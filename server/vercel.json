{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }
  ],
  "scripts": {
    "build": "npm install && npm run postinstall"
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.js" }
  ]
}
